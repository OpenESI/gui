<wizard>
	<step id="restorequestion">
		<condition>
from Plugins.SystemPlugins.SoftwareManager.ImageWizard import checkConfigBackup
self.backuppath = checkConfigBackup()
self.condition = (self.backuppath is not None and config.misc.firstrun.value)
		</condition>
		<text value="Vuoi ripristinare le tue impostazioni?" />
		<displaytext value="Vuoi ripristinare le tue impostazioni?" />
		<list>
			<listentry caption="Sì, ripristina le impostazioni ora" step="restore" />
			<listentry caption="No, avvia il mio %s %s" step="end" />
		</list>
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
		</code>
	</step>

	<step id="welcome">
		<text value="Benvenuto nella procedura di aggiornamento firmware. Questa guida ti assisterà nell'aggiornamento del tuo %s %s fornendo un sistema di backup delle impostazioni e spiegazioni per l'aggiornamento." />
		<displaytext value="Vuoi la guida all'aggiornamento?" />
		<list>
			<listentry caption="OK, guidami nell'aggiornamento" step="backupquestion" />
			<listentry caption="Esci dalla procedura" step="end" />
		</list>
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
		</code>
	</step>

	<step id="backupquestion">
		<text value="La procedura può salvare le tue impostazioni attuali. Vuoi eseguire un backup ora?" />
		<displaytext value="Vuoi eseguire un backup ora?" />
		<list>
			<listentry caption="Sì, salva le mie impostazioni!" step="backupwhere" />
			<listentry caption="Non serve backup" step="upgrade" />
		</list>
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
		</code>
	</step>

	<step id="backupwhere" nextstep="backup">
		<text value="Dove vuoi salvare il backup delle impostazioni?" />
		<displaytext value="Dove vuoi salvare il backup?" />
		<list type="dynamic" source="listDevices" evaluation="deviceSelectionMade" onselect="deviceSelectionMoved" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
		</code>
	</step>

	<step id="backup" nextstep="backupresult">
		<text value="Hai scelto di salvare le impostazioni. Premi OK per avviare il backup." />
		<displaytext value="Premi OK per avviare il backup" />
		<config screen="BackupScreen" module="Plugins.SystemPlugins.SoftwareManager.BackupRestore" type="ConfigList" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
self.selectKey("LEFT")
self.selectKey("RIGHT")
		</code>
		<code pos="after" async="yes">
self.currStep = self.getStepWithID('backupresult')
self.afterAsyncCode()
		</code>
	</step>

	<step id="backupresult" nextstep="upgrade">
		<condition>
from Plugins.SystemPlugins.SoftwareManager.ImageWizard import checkBackupFile
self.backuppath = checkBackupFile()
self.condition = (self.backuppath is True)
		</condition>
		<text value="Backup eseguito con successo. Procediamo con le spiegazioni per l'aggiornamento." />
		<displaytext value="Backup completato" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
		</code>
	</step>

	<step id="backupresult" nextstep="backupwhere">
		<condition>
from Plugins.SystemPlugins.SoftwareManager.ImageWizard import checkBackupFile
self.backuppath = checkBackupFile()
self.condition = (self.backuppath is False)
		</condition>
		<text value="Backup fallito! Scegli un'altra posizione per il backup." />
		<displaytext value="Backup fallito" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
		</code>
	</step>

	<step id="restore" nextstep="backupresult">
		<text value="Hai scelto di ripristinare le impostazioni. Enigma2 si riavvierà dopo il ripristino. Premi OK per iniziare." />
		<displaytext value="Ripristina impostazioni ora" />
		<config screen="RestoreScreen" module="Plugins.SystemPlugins.SoftwareManager.BackupRestore" type="ConfigList" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
self.selectKey("LEFT")
self.selectKey("RIGHT")
		</code>
	</step>

	<step id="upgradehow">
		<text value="Seleziona la posizione da cui ottenere i file di aggiornamento." />
		<displaytext value="Seleziona sorgente aggiornamento" />
		<list>
			<listentry caption="Installa nuova immagine via browser web" step="upgrade" />
			<listentry caption="Installa nuova immagine via USB" step="upgradeUSB" />
		</list>
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
		</code>
	</step>

	<step id="upgrade">
		<text value="Ti serve un PC connesso al tuo %s %s. Visita il sito https://www.openesi.eu/ per le istruzioni complete.\nIl %s %s si spegnerà. Dopo aver seguito le istruzioni sul sito, il nuovo firmware ti chiederà di ripristinare le impostazioni." />
		<displaytext value="Procedere con l'aggiornamento?" />
		<list>
			<listentry caption="Sì, spegni ora" step="shutdown" />
			<listentry caption="No, non fare nulla" step="end" />
		</list>
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
		</code>
	</step>

	<step id="shutdown" nextstep="shutdown">
		<code>
from enigma import quitMainloop
from Screens.Standby import QUIT_SHUTDOWN
quitMainloop(QUIT_SHUTDOWN)
		</code>
		<text value="Il %s %s si sta spegnendo. Attendere..." />
		<displaytext value="Spegnimento in corso..." />
	</step>

	<step id="end" laststep="true">
		<condition>
self.condition = self.isLastWizard
		</condition>
		<text value="Procedura guidata terminata." />
		<displaytext value="Procedura completata" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
		</code>
	</step>
</wizard>
